/*Arrumar o JavaScript*/ 

const botao = document.getElementById("cancelBtn");
const painel = document.getElementById('painelLogin');

/*botao.addEventListener("click", ()=> {
    painel.style.animation = "";
    setTimeout(() => painel.style.animation = "animatezoom 1s", 5);
});

window.onclick = function(event) {
    if (event.target == painel) {
        painel.style.display = "none";
    }
}*/